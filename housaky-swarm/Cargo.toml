[package]
name = "housaky-swarm"
version = "2.0.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.42", features = ["full"] }
rand = "0.8"

[dev-dependencies]
